{
  "name": "fsopen-backend-part3b",
  "version": "1.0.0",
  "description": "",
  "homepage": "https://github.com/M-Ayoob-A/FSOpen-Backend-part3b#readme",
  "bugs": {
    "url": "https://github.com/M-Ayoob-A/FSOpen-Backend-part3b/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/M-Ayoob-A/FSOpen-Backend-part3b.git"
  },
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../frontend && npm run build && cp -r dist ../backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "mongoose": "^9.0.0",
    "morgan": "^1.10.1"
  }
}
